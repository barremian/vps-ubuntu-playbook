---
- name: Update apt cache and upgrade packages
  ansible.builtin.apt:
    update_cache: true
    upgrade: dist

- name: Install base dependencies
  ansible.builtin.apt:
    name: "{{ common_packages }}"
    state: present
